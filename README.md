## Use this skill in your project (OpenCode)

Run this in **your project's root directory** (the directory where you run `opencode`):

```bash
curl -fsSL https://raw.githubusercontent.com/0xUniko/vibe-browser/main/scripts/install-opencode-skill.sh | bash
